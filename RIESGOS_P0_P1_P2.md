# RIESGOS — P0 / P1 / P2

P0 impide funcionar, compromete seguridad/datos o rompe arquitectura · P1 corre pero rompe metodología, profundidad o confiabilidad · P2 mejora posterior.
Cada ítem: dónde, cómo se demuestra, corrección propuesta. Ninguno está corregido salvo los marcados **[corregido en auditoría]**.

## P0 (6 — todos CERRADOS en la fase de corrección; ver tabla al final)

| ID | Riesgo | Dónde | Demostración | Corrección |
|---|---|---|---|---|
| P0-01 | **Las RPC de la cola son ejecutables por cualquier usuario autenticado.** `take_job`, `recover_stale_jobs`, `refresh_company_stats` son `security definer` y Supabase concede `EXECUTE` a `anon`/`authenticated` por defecto. Un cliente con sesión puede robar trabajos, marcarlos o refrescar la vista. | `supabase/schema.sql` §FUNCIONES DE COLA | Con el anon key + JWT de cliente: `supabase.rpc('take_job')` devolvería un job con `payload` de otra empresa. | `revoke execute on function take_job(int), recover_stale_jobs(), refresh_company_stats() from public, anon, authenticated; grant execute … to service_role;` |
| P0-02 | **RLS deja que el cliente lea `claims.estado`, `pilar`, `contradice_a`, `explicacion_contradiccion` con el anon key.** El spec exige "solo el texto, para validar. Nunca el estado ni el pilar" y "no se implementa filtrando en el frontend". Hoy la frontera vive solo en `/api/companies/[id]/reality`. | `schema.sql` política `cliente_claims` (select sin restricción de columnas); `api/companies/[id]/reality/route.ts:25` | `supabase.from('claims').select('*')` desde el navegador del dueño devuelve todo. | Quitar `cliente_claims`; crear vista `claims_cliente(id, company_id, texto, source_id, fragment_id, fecha_afirmacion, prioridad_validacion)` con `security_invoker` y política por membresía; o `revoke select (estado, pilar, …) on claims from authenticated`. |
| P0-03 | **El dueño puede leer las respuestas de sus empleados, con `participant_id` → nombre.** Rompe "protección del que habla" (7.4, 36). | `schema.sql` `cliente_sessions`, `cliente_responses` (todas las sesiones de la empresa) | `from('interview_responses').select('*, interview_sessions(participants(nombre))')` con JWT del dueño. | Política: solo sesiones cuyo `participants.user_id = auth.uid()`. El portal usa service role y ya filtra; la política de cliente sobra. |
| P0-04 | **`participants.token` visible para el cliente.** Con el token de un empleado el dueño responde por él. | `schema.sql` `cliente_participants` (select de toda la fila) | `from('participants').select('token')`. | Quitar la política o `revoke select (token) on participants from authenticated`. |
| P0-05 | **Guardar el canvas pierde las conexiones de los nodos nuevos.** `Canvas.guardar` envía `id: undefined` + `_tmp` para nodos `tmp-*`; el PUT hace `mapa.set(n.id ?? nuevo.id, nuevo.id)` → la clave es el id nuevo, no el temporal; luego `mapa.get(e.origen)` para `tmp-3` es `undefined` y la arista se descarta. "Tú dibujas" no funciona para nada dibujado a mano. | `src/app/api/processes/[id]/route.ts:45` · `src/components/canvas/Canvas.tsx:89` | Agregar dos nodos, conectarlos, guardar, recargar: aparecen sin conexión. (NP sin navegador; lectura del código es concluyente.) | En el PUT: `const tmp = (n as {_tmp?:string})._tmp ?? n.id; mapa.set(tmp, nuevo.id)`. Añadir `_tmp` al tipo `NodoIn`. |
| P0-06 | **Un cliente puede conducir y leer cualquier sesión de su empresa por API.** `/api/companies/[id]/interview/next` (GET/POST) y `/api/interviews/[sesion]/answer` solo exigen `puedeAcceder(companyId)`; `estadoSesion` devuelve `respondidas` con texto. El dueño ve y contesta las entrevistas del personal. | `src/app/api/companies/[id]/interview/next/route.ts:14`; `src/app/api/interviews/[sesion]/answer/route.ts:12` | POST con `session_id` de la sesión de Rosa desde la cuenta del dueño → 200. | Si `perfil.rol !== 'consultor'`: exigir `participants.user_id === perfil.id` para la sesión. |

## P1 (20 + 2 nuevos: P1-21, P1-22)

| ID | Riesgo | Dónde | Demostración | Corrección |
|---|---|---|---|---|
| P1-01 | **La voz del equipo no entra al diagnóstico.** Los claims de empleados nacen `sin_verificar`; `portal/validar` los excluye (`!x.participant_id`); el consultor solo puede confirmarlos uno a uno en la tabla. El diagnóstico usa `confirmado/contradicho/caducado`. Sin intervención manual, el contraste dueño–equipo (corazón de El Espejo) no alimenta hallazgos. | `handlers/extraer.ts:149` (`estado: rol==='dueno' ? 'confirmado' : 'sin_verificar'`); `portal/validar/page.tsx:26`; `handlers/diagnostico.ts:25` | `flujo-demo.test.ts` §10: `c-diego-descuentos` sigue `sin_verificar` tras toda la validación. | Regla: un claim de entrevista lo sostiene quien lo dijo → `confirmado` con `participant_id`, y la independencia de fuentes (`rules/evidencia.ts`) ya protege contra opiniones únicas. O validación por el consultor en bloque. |
| P1-02 | **Sueño del dueño sin profundidad garantizada.** El prompt lista 6 bloques pero no hay control de cobertura; faltan "qué sí amas hacer", "qué es suficiente", "qué quieres dejar construido", "¿esta empresa te lleva a esa vida?". La sesión cierra cuando el LLM devuelve `[]`. | `agents/entrevistador.ts` GUÍA; `handlers/entrevista.ts:79` | No hay test posible sin IA; lectura del código. | Tabla `bloques_requeridos[tipo]`; `sesion_completa` solo si todos tienen respuesta; añadir preguntas faltantes. |
| P1-03 | **Entrevista al personal cubre 8 de 14 preguntas mínimas.** Faltan: resultado esperado, cómo sabe si lo hizo bien, información que falta, decisión que escala, sistema paralelo, qué escucha de clientes, qué conservaría, qué ve que Dirección no ve. | `agents/entrevistador.ts` bloques personal | Comparar prompt con la lista. | Ampliar banco y cobertura. |
| P1-04 | **Condición de salida 7.9 no existe en código.** Nada comprueba que ningún claim de tipo crítico quede sin verificar; las etapas avanzan por efectos colaterales o a mano. El "modo intensivo" no tiene condiciones de suficiencia codificadas (solo `confirmadas ≥ 20` en la bandeja). | `handlers/entrevista.ts`; `lib/bandeja.ts:37`; `Etapa.tsx` | — | `lib/rules/suficiencia.ts`: `levantamientoCompleto(claims)`, `diagnosticoListo(findings)`; usarla en entrevistador, bandeja y avance de etapa. |
| P1-05 | **Know-how parcial:** sin proceso/actividad/criticidad/documentado/rol; sin FK a `process_nodes`; el destino es una etiqueta sin ruta (no genera checklist, entrenamiento ni ficha de agente); `riesgo_know_how_vacio` se devuelve y se pierde. | `schema.sql` know_how; `handlers/entrevista.ts` handleMinarKnowHow | `flujo-demo` §9 prueba el modelo, no el uso. | Columnas + FK; al recibir `riesgo: true` crear finding `informacion_insuficiente` en personas. |
| P1-06 | **Evidencia exacta incompleta por formato.** Audio: `audio_desde/hasta` nunca se llenan (Whisper sin timestamps; Web Speech no los da). CSV: `celda` nunca se llena (el extractor recibe texto plano). Entrevista: el claim no guarda `response_id` (solo `seccion: "Pregunta N"`). | `handlers/extraer.ts` (fragment insert), `anthropic.ts` transcribe | `flujo-demo` §1 pasa porque el fixture los trae; el runtime no los produce. | Whisper `verbose_json` con segmentos; extractor de CSV con fila/columna; `claims.response_id`. |
| P1-07 | **Validación de grafos no conectada.** `rules/grafo.ts` existe (creado en auditoría) pero ni el PUT ni el ARQUITECTO la llaman: se guardan decisiones con una salida, procesos sin fin, `remove` con dependientes, `ia` sobre nodos "?". | `api/processes/[id]/route.ts`; `handlers/procesos.ts` | `grafo.test.ts` prueba la regla aislada. | Llamar `validarFlujograma` en PUT (avisos, no bloqueo) y en `guardarFlujograma` (marcar `problema`). |
| P1-08 | Nodos sin rol, entrada, salida, evidencia, estándar, know-how. El SOP los pide y el LLM los inventa. | `schema.sql` process_nodes; `PanelPropiedades.tsx` | — | Columnas + panel. |
| P1-09 | **`requiere_validacion` no es un estado.** Vive en `findings.filtros` jsonb; no hay badge, no bloquea "Aprobar". Un hallazgo derribado por el AUDITOR queda como "bajo" y puede aprobarse. | `handlers/diagnostico.ts:100`; `Hallazgo.tsx` | `evidencia.test.ts` calcula el flag; nadie lo lee. | Columna `findings.requiere_validacion`; UI; `review` exige motivo si está activo. |
| P1-10 | **Reintento de un tramo duplica afirmaciones.** La idempotencia es por job; los `claims` insertados antes de un fallo (p. ej. a mitad del bucle) quedan y el reintento los vuelve a insertar. | `handlers/extraer.ts:84-107` | — | `delete from claims where source_id=? and fragment.seccion like 'tramo i'` antes de insertar, o `claims.idempotency_key` unique. |
| P1-11 | **Audio sin `OPENAI_API_KEY` bloquea la sesión.** `transcribir_respuesta` falla 3 veces → `fallido`; la respuesta queda con `respuesta_audio_path` y `respuesta null`; el participante ve "Escuchando tu respuesta" para siempre. Ocurre en Firefox/Safari viejos (sin SpeechRecognition). | `handlers/entrevista.ts` handleTranscribirRespuesta; `BotonGrabar.tsx` fallback | — | Si no hay transcriptor: no ofrecer `alAudio`; en fallo, limpiar `respuesta_audio_path` y pedir texto. |
| P1-12 | **Realtime no llega al cliente** (sin política RLS en `jobs`/`claims` → `postgres_changes` filtra todo); cae a sondeo de 4 s. La bandeja y el portal dependen de `company_stats`, que solo refresca el worker (empresa nueva invisible en "atención hoy" hasta 60 s; sin worker, nunca). | `Progreso.tsx`; `lib/bandeja.ts`; `lib/portal.ts` | — | Política de lectura de `jobs` propios; refrescar la vista al crear empresa; fallback a conteo directo si la vista está vacía. |
| P1-13 | **Lease fijo de 10 min sin heartbeat.** Un PDF grande tarda más → `recover_stale_jobs` lo devuelve a pendiente mientras sigue corriendo → doble ejecución y duplicados. | `worker.ts` LEASE; `schema.sql` take_job | — | Heartbeat `update jobs set lease_expira_at=now()+10min` cada 60 s desde el worker. |
| P1-14 | **PDF e imagen no se trocean.** Una sola llamada con el archivo completo; el proveedor limita páginas/MB; un manual de 120 páginas falla entero. | `handlers/extraer.ts:62-70` | — | Partir PDF por páginas (pdf-lib) en tramos de ~20 páginas, un job por tramo con `pagina` base. |
| P1-15 | **El contraste pisa confirmaciones del dueño sobre documentos.** Solo protege claims con `participant_id`; un claim de documento que el dueño confirmó ("Sigue siendo verdad") vuelve a `contradicho` en la siguiente corrida. | `handlers/contrastar.ts:39` | — | Saltar claims con `validado_por` no nulo. |
| P1-16 | **Sin flujo de invitación.** El cliente solo se enlaza si su email existía al crear la empresa; si se registra después, ve "tu consultor no te enlazó" sin salida. Tokens de participantes sin expiración ni revocación. | `api/companies/route.ts:24`; `participants.token` | — | Ruta `POST /api/companies/[id]/invitar` + tabla `invitations`; `participants.expira_at`. |
| P1-17 | **Monitoreo incompleto.** Sin alertas "semana cerrada con pendientes" ni "KPI sin moverse 30 días"; regresiones en cortes no crean hallazgos; sin agenda semanal de 30 min; fase `monitoreo` sin acciones. | `lib/bandeja.ts`; `api/companies/[id]/close` | — | Reglas en `bandeja.ts`; corte con `regresiones[]` → findings. |
| P1-18 | **Entregables sin congelar ni verificar.** Mapas, manual y plan leen datos vivos al verse (un cambio posterior altera lo "publicado"); "toda afirmación cita su fuente" solo en prompt; marca mostrada = empresa cliente, no la consultoría. | `VistaEntregable.tsx`; `handlers/plan.ts` handleRedactarEntregables | — | Snapshot JSON al publicar; post-check: cada sección con `fuentes.length>0`; config `MARCA_CONSULTORIA`. |
| P1-19 | **Consolidación cross-pilar ausente.** Pasadas B (conectar) y F (prioridad) corren por pilar; no hay job que una los 4 y ordene por multiplicación. `transversal` se repite en los 4. | `handlers/diagnostico.ts` | — | Job `consolidar` tras los 4 pilares (dedupe + orden). |
| P1-20 | **Límites no globales.** Concurrencia 6 por proceso (N workers = 6N llamadas); sin límite por empresa; `superaTope` suma `token_usage` completo en cada job; tope solo aplica a p≥5 (una entrevista puede agotar cuota sin freno). | `worker.ts:28,42`; `db/queries.ts` superaTope | — | Semáforo en Postgres (`pg_advisory_lock` o fila `limites`); vista agregada de tokens. |

## P2 (16 + 2 nuevos: P2-17, P2-18)

| ID | Riesgo | Dónde | Corrección |
|---|---|---|---|
| P2-01 | `estadoSesion.pendienteTranscripcion` siempre `false`: el `select` no incluye `respuesta_audio_path`. | `lib/entrevista.ts:39` | Añadir columna al select. |
| P2-02 | `hallazgosAprobadosConEvidencia` ordena `impacto` alfabéticamente (alto, bajo, medio). | `db/queries.ts` | `order` por CASE o en JS. |
| P2-03 | Carrera al terminar tramos: dos chunks simultáneos ven "0 pendientes" y encolan dos `contrastar` (coste doble, no error). | `handlers/extraer.ts:111` | Clave idempotente por `source_id` + versión de claims. |
| P2-04 | Borrar empresa/fuente deja archivos huérfanos en Storage (cascade no toca el bucket); borrar un usuario falla por FK `participants.user_id` sin `on delete`. | `schema.sql` | Trigger de limpieza; `on delete set null`. |
| P2-05 | Cliente con varias empresas: `empresaDelCliente` toma la primera. Sin `workspace` (una sola consultoría): aceptable para el primer caso. | `lib/auth.ts` | Selector de empresa; `workspaces` en fase 2. |
| P2-06 | `proxy.ts` llama `auth.getUser()` (red) en cada request incluidas las API; rutas no públicas sin sesión reciben redirect HTML, no 401. | `src/proxy.ts` | `getSession` + 401 para `/api/*`. |
| P2-07 | `speak()` en el proveedor no se usa (código muerto); `preguntaDeVigencia` tampoco. | `lib/ai/anthropic.ts`; `rules/vigencia.ts` | Quitar o usar. |
| P2-08 | `participants.rol` admite `cliente`/`consultor` sin UI ni sesiones; sesión `validacion` nunca se crea automáticamente. | `Participantes.tsx`; `api/participants` | Crear sesión `validacion` al detectar contradicciones. |
| P2-09 | Constantes hardcoded: MIN_CONFIRMADAS=5, 20 validaciones por pantalla, 60 pares, DIAS_TRABADA=5, tope 2M tokens, lease 10, `confirmadas ≥ 20`. | varios | `lib/config.ts`. |
| P2-10 | El cliente puede borrar fuentes (y sus claims en cascada) y participantes. | `api/sources/[id]/extract` DELETE; `api/participants` DELETE | Solo consultor, o papelera. |
| P2-11 | "Agente de IA" visible en el panel del cliente (`EJECUTOR.ia`). | `textos.ts`; `PanelPropiedades.tsx` | Texto alterno: "automático". |
| P2-12 | Seguimiento de validación ("¿qué cambió?") se guarda como `observacion` sin extraer claims. | `api/claims/[id]/validate` | Encolar `extraer`. |
| P2-13 | README promete lectura de notas de WhatsApp y PDF escaneado; lo primero exige OPENAI_API_KEY, lo segundo depende del proveedor (sin troceo). | `README.md` | Precisar. |
| P2-14 | `company_stats` recalcula 10 subconsultas × N empresas cada minuto (ok hasta ~100 empresas). | `schema.sql` | Índices ya existen; revisar a 100+. |
| P2-15 | `elementsSelectable={!soloLectura \|\| true}` es siempre `true` (inocuo). | `Canvas.tsx` | Simplificar. |
| P2-16 | Edges desde nodos decisión generados por IA no traen `sourceHandle`; React Flow podría no dibujarlos (NP). | `Canvas.tsx` aRF; `nodos.tsx` handles | Asignar `sourceHandle` por etiqueta sí/no al cargar. |

## Corregido durante la auditoría (sin cambiar comportamiento deseado)

- **vigencia**: `VIDA_UTIL_MESES[tipo] ?? 24` convertía `proposito: null` ("nunca caduca") en 24 meses; y foto/dato sin fecha no disparaban validación. `src/lib/rules/vigencia.ts`, cubierto por `tests/vigencia.test.ts`.
- Extraídas a módulos puros (mismo comportamiento, ahora probado): `rules/evidencia.ts`, `rules/plan.ts`, `jobs/reglas.ts`; handlers `diagnostico.ts`, `plan.ts` y `worker.ts` los usan. `rules/grafo.ts` es nuevo y **no está conectado** (P1-07).


---

# FASE DE CORRECCIÓN P0 — 2026-08-22 · P0 restantes: 0

| ID | ANTES | CORRECCIÓN | ARCHIVOS MODIFICADOS | TEST QUE LO DEMUESTRA | ESTADO FINAL |
|---|---|---|---|---|---|
| P0-01 | `take_job`, `recover_stale_jobs`, `refresh_company_stats` eran `security definer` con el `EXECUTE` por defecto para `anon`/`authenticated`: cualquier usuario con sesión podía robar trabajos de otra empresa. | `revoke execute … from public, anon, authenticated` + `grant … to service_role` para las 3 funciones y para la nueva `guardar_proceso`. `es_consultor()`/`mis_empresas()` se conceden explícitamente a `authenticated` porque las usan las políticas. | `supabase/schema.sql` (§PERMISOS DE FUNCIONES) | `tests/p0-schema.test.ts` › P0-01 (6 tests: revoke+grant por función, ninguna concesión a anon/authenticated, security definer confirmado) · integración real: `tests/pendientes.test.ts` [P0-01] | **CERRADO** (estático) · integración pendiente de Supabase |
| P0-02 | Política `cliente_claims` (select sin restricción de columnas): con el anon key el dueño leía `estado`, `pilar`, `contradice_a`, `explicacion_contradiccion`, `pregunta_sugerida`. La frontera vivía solo en una ruta. | Se elimina la política. Se crea la vista `claims_cliente` (security_barrier, filtro de pertenencia dentro) con exactamente: `id, company_id, texto, fecha_afirmacion, fuente_nombre, fuente_tipo, requiere_validacion, pregunta, opciones`; excluye lo dicho por otras personas. `revoke all … from public, anon; grant select … to authenticated`. En servidor, `src/lib/frontera.ts` (`filaCliente`, `visibleParaCliente`, `sinColumnasInternas`) es el espejo exacto y la ruta `/reality` lo usa con una aserción que lanza si una columna interna se cuela. | `supabase/schema.sql` (§VISTAS SEGURAS), `src/lib/frontera.ts` (nuevo), `src/app/api/companies/[id]/reality/route.ts` | `tests/p0-schema.test.ts` › P0-02 (4) · `tests/p0-frontera.test.ts` (5) · integración: `pendientes` [P0-02] | **CERRADO** |
| P0-03 | `cliente_sessions`/`cliente_responses` permitían al dueño leer respuestas de empleados con `participant_id` → nombre. Además `/api/sources/[id]` abría transcripciones de entrevista y `portal/resultados` serializaba `participants.nombre` al HTML. | Políticas reescritas: solo sesiones/respuestas cuyo `participants.user_id = auth.uid()`. `/api/sources/[id]` devuelve 403 a clientes para `tipo='entrevista'`. `portal/resultados` vacía `participants.nombre` antes de pasar al componente. La vista `claims_cliente` y `/reality` excluyen claims con `participant_id` ajeno. | `supabase/schema.sql` (§RLS), `src/app/api/sources/[id]/route.ts`, `src/app/(cliente)/portal/resultados/page.tsx`, `src/app/api/companies/[id]/reality/route.ts` | `tests/p0-schema.test.ts` › P0-03 (3) · `tests/p0-frontera.test.ts` › "lo que dijo otra persona no es visible" · integración: `pendientes` [P0-03] | **CERRADO** |
| P0-04 | `participants.token` en claro, generado por `default`, legible por el cliente vía política `cliente_participants`, devuelto en el panorama al componente y copiable desde la UI. Sin expiración, sin revocación, sin tope de usos. | Columna `token` eliminada; `token_hash` (sha256), `token_expira_at` (30 días), `token_revocado_at`, `token_usos`/`token_max_usos` (200). Token de 192 bits (`randomBytes`, base64url) generado en `POST /api/participants` y en la nueva `POST /api/participants/[id]/enlace` (rotar / `{revocar:true}`); el plano viaja **una sola vez** en esa respuesta. `/api/participar/[token]` valida formato, busca por hash (`timingSafeEqual`), comprueba revocación, vencimiento y usos; incrementa usos por respuesta; 404 uniforme. Política `cliente_participants` eliminada; vista `participants_cliente` sin hash ni `user_id`. Panorama ya no selecciona el token. Logs de `api.ts` redactan `/participar/<token>`. | `supabase/schema.sql`, `src/lib/tokens.ts` (nuevo), `src/app/api/participants/route.ts`, `src/app/api/participants/[id]/enlace/route.ts` (nuevo), `src/app/api/participar/[token]/route.ts`, `src/components/consultor/Participantes.tsx`, `src/app/(consultor)/empresa/[id]/page.tsx`, `src/lib/api.ts` | `tests/p0-tokens.test.ts` (11: aleatoriedad, hash, vencido, revocado, usos, formato manipulado, redacción en logs) · `tests/p0-schema.test.ts` › P0-04 (2) · grep: 0 referencias a `.token` en `src/` fuera de `tokens.ts` | **CERRADO** · residual P2-17 (token en URL) |
| P0-05 | `/interview/next` y `/interviews/[sesion]/answer` solo exigían acceso a la empresa: el dueño leía y contestaba sesiones de empleados. | `src/lib/sesiones.ts`: `autorizaSesion(actor, sesion, accesoEmpresa)` → consultor sí; usuario solo si `participants.user_id === auth.uid()`; empresa ajena o inexistente → 404 uniforme; ajena dentro de la empresa → 403. Ambas rutas lo usan; `/interview/next` además exige que la sesión pertenezca al `[id]` de la URL. `/api/participar` ya limitaba a la sesión del token. `lib/portal.ts` enlaza al dueño con su participante (`user_id`) la primera vez para que su propia conversación siga funcionando. | `src/lib/sesiones.ts` (nuevo), `src/app/api/companies/[id]/interview/next/route.ts`, `src/app/api/interviews/[sesion]/answer/route.ts`, `src/lib/portal.ts` | `tests/p0-sesiones.test.ts` (10: dueño→empleada 403, empleado A→B 403, propia ✔, consultor ✔, otra empresa 404, inexistente 404, company_id manipulado ignorado) | **CERRADO** |
| P0-06 | `PUT /api/processes/[id]` construía el mapa con `n.id ?? nuevo.id`; los nodos nuevos llegaban con `id: undefined` → la clave era el uuid nuevo, no el `_tmp`, y toda conexión que tocara un nodo nuevo se descartaba. Además el guardado era una secuencia de consultas sin transacción. | Función SQL `guardar_proceso(process_id, nombre, area, nodos jsonb, edges jsonb)` (plpgsql = una transacción): upsert de nodos, mapa `_tmp→uuid`, borrado de nodos ausentes, borrado y reinserción de conexiones con ids resueltos; `raise exception 'conexion_invalida'` si una conexión apunta a un id no resuelto → rollback completo. La ruta valida antes (`validarEntrada`: tipos, `_tmp` obligatorio en nuevos, referencias resolubles; ids de otros procesos se tratan como nuevos) y llama a la RPC con service role. `src/lib/canvas-guardar.ts` contiene el algoritmo de referencia en memoria con la misma semántica. | `supabase/schema.sql` (§GUARDADO ATÓMICO), `src/lib/canvas-guardar.ts` (nuevo), `src/app/api/processes/[id]/route.ts` | `tests/p0-canvas.test.ts` (13: los 10 casos pedidos + validación previa + canvas manipulado + ronda compleja de 7 conexiones) · `tests/p0-schema.test.ts` › P0-05 · e2e navegador: `pendientes` [P0-05] | **CERRADO** (lógica + SQL) · e2e pendiente de navegador |

(Nota: en la auditoría original el bug del canvas era P0-05 y la autorización de sesiones P0-06; aquí se numeran según el pedido de corrección: sesiones = P0-05, canvas = P0-06.)

## Re-auditoría adversarial tras la corrección

| Ataque | Resultado |
|---|---|
| Modificar `company_id` en `/interview/next` | La empresa sale de la sesión cargada por id, no del cliente; si no coincide con la URL → 404. `p0-sesiones` › "company_id manipulado". |
| Modificar `session_id` (empleado de mi empresa) | 403. Otra empresa o inexistente → 404 sin revelar existencia. |
| Usar el token de otra sesión en `/api/participar` POST | `participant_id !== p.id` → 403. |
| Llamar `rpc('take_job')` con JWT de cliente | `EXECUTE` revocado → 42501 (estático verificado; integración pendiente). |
| `from('claims').select('estado')` con JWT de cliente | Sin política → 0 filas. `from('claims_cliente').select('estado')` → columna inexistente → error. |
| Inspeccionar la respuesta de `/api/companies/[id]/reality` como cliente | Solo las 9 claves de `FilaCliente`; la ruta lanza si aparece una interna. Claims de empleados ausentes. |
| Obtener `participants.token_hash` | Sin política de cliente; la vista no tiene la columna; ninguna ruta la devuelve. |
| Reutilizar token vencido / revocado / agotado | 404 uniforme. |
| Guardar canvas con ids de otro proceso o conexiones a ids inventados | Ids ajenos se tratan como nodos nuevos del proceso propio; conexiones a desconocidos → 400 antes de tocar la base; si aun así llega → exception → rollback. |
| Abrir la transcripción de un empleado vía `/api/sources/[id]` desde el portal | 403 para `tipo='entrevista'`. |
| Leer `participants.nombre` en el HTML de `portal/resultados` | Vaciado en servidor antes de serializar. |

## Nuevos riesgos registrados durante la corrección (NO corregidos, fuera de alcance P0)

| ID | Riesgo | Dónde | Nivel |
|---|---|---|---|
| P1-21 | `POST /api/claims/[id]/validate` deja que un cliente cambie el estado de un claim de un empleado si conoce el uuid (no se le muestra, pero la ruta no lo impide). Integridad, no lectura. | `src/app/api/claims/[id]/validate/route.ts` | P1 |
| P1-22 | `DELETE /api/sources/[id]/extract` permite a un cliente borrar una fuente `entrevista` de un empleado (y sus claims en cascada). | `src/app/api/sources/[id]/extract/route.ts` | P1 |
| P2-17 | El token viaja en la URL (`/participar/<token>`): historial del navegador y logs de acceso. Mitigado por expiración 30 días + revocación + tope de usos. Alternativa futura: token en cuerpo + cookie de participante. | `src/app/participar/[token]` | P2 |
| P2-18 | Las vistas `claims_cliente`/`participants_cliente` corren con privilegios del dueño (sin `security_invoker`); el linter de Supabase lo señalará. Intencional: el filtro está dentro. Documentar al conectar. | `supabase/schema.sql` | P2 |
